import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sarkariyojana.app',
  appName: 'government-schemes-pwa',
  webDir: 'www',
};

export default config;
