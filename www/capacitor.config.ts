import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.government.schemes',
  appName: 'Government Schemes India',
  webDir: 'www'
};

export default config;
